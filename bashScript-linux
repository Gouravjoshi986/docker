docker build -t linux-img -f Dockerfile.linux .  
// we use this command to build a docker image for ubuntu(linux) with name linux-img 
-t to give it a tag  // . is used to specify the current running directory 

docker run -it linux-img 
// -it is used for interactive terminal 